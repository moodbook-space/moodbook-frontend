version: '3'
services:
  front:
    build: 
      context: .
    container_name: moodbook-frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt/archive/moodbook.live/fullchain1.pem:/etc/ssl/certs/fullchain.pem:ro
      - /etc/letsencrypt/archive/moodbook.live/privkey1.pem:/etc/ssl/private/privkey.pem:ro